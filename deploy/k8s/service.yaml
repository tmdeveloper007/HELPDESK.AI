apiVersion: v1
kind: Service
metadata:
  name: helpdesk-backend
  labels:
    app: helpdesk-backend
spec:
  type: ClusterIP
  selector:
    app: helpdesk-backend
  ports:
    - name: http
      port: 80
      targetPort: http
